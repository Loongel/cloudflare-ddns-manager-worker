{
  "name": "cloudflare-ddns-manager-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker DDNS manager with one-click deploy, KV-backed scoped tokens, and a cron-friendly client.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "deploy:test": "scripts/deploy-and-test.sh",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
